// Re-export sonner's Toaster for use as a shadcn-compatible toast component.
// Use the `toast` function from "sonner" directly in components.
export { Toaster } from "sonner";
export { toast } from "sonner";
